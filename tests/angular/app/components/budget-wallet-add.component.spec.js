ngDescribe({
    name: 'Test budget-wallet-add component',
    modules: 'app',
    element: '<budget-wallet-add></budget-wallet-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
