ngDescribe({
    name: 'Test project-detail component',
    modules: 'app',
    element: '<project-detail></project-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
