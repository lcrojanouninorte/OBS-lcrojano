ngDescribe({
    name: 'Test challenge-add component',
    modules: 'app',
    element: '<challenge-add></challenge-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
