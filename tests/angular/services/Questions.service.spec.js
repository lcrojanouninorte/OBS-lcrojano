ngDescribe({
    name: 'Test QuestionsService',
    modules: 'app',
    inject: 'QuestionsService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
